export async function getEmbedding(text: string): Promise<number[] | null> {
  // Replace with call to real embedding model e.g. OpenAI, local model, or cohere
  // Example pseudo-implementation
  return [0.123, 0.456, 0.789]; // Dummy vector
}
